import org.junit.Test
import kotlin.test.assertEquals

class MainTest {
    @Test fun addition() {
        val calculation = 1 + 1
        val result = 2
        assertEquals(calculation, result)
    }
}